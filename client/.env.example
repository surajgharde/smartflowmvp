# Only needed when the API is hosted somewhere other than the same origin as
# the front end. Leave this unset for local development and for the default
# single-project Vercel deployment — both serve the API at /api already.
#
# VITE_API_URL=https://smartflow-api.onrender.com/api
